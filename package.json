{
  "name": "setinterval-limit",
  "version": "1.0.0",
  "description": "If you need call method repeatedly but you need avoid over called. You can configure to called just if change and/or called just if is called to call (every interval or every called but in minimal setted interval)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["setInterval", "diff interval"],
  "author": "Renato Seiji Miawaki",
  "license": "ISC"
}
